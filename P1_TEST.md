# Card Duel — P1 对战核心 · 测试清单与潜在 Bug

> 生成于 P1 全部代码完成后（提交 `f3d4a0d`）。P1 = 对局框架 + 俯视交互 + 出牌攻击 + 换牌硬币。
> **2025 增补（四类结算区分）**：P1 引擎已按卡牌类型分流结算（召唤站场/魔法即时/陷阱秘密/装备附着），内建技能最小集见 `CARD_DESIGN.md`。
> 测试状态：**P1-1a/P1-1b 已验证通过**；P1-2/P1-3/P1-4 及四类结算尚未实测（缺少双客户端与足够的卡牌）。

---

## 0. 测试前置条件

### 0.1 需要两个游戏实例
- 方式 A：`gradlew runServer` + 两个客户端（`gradlew runClient` 或已安装 NeoForge 1.21.1 的启动器）连接 localhost
- 方式 B：单机开世界 → 暂停菜单"对局域网开放" → 第二个客户端加入（注意：局域网联机时两个客户端都要装本 mod）

### 0.2 卡牌数量问题（当前主要阻塞）
- 开一局需要 **2 副 × 27 张 = 54 张卡**，且卡包必须装满 27 张才能提交
- 默认卡包目前只有 **1 张"羊"**（`trap` 类型、skill=0 → 走"未知技能站场兜底"，行为等同召唤卡）
- 获取 54 张羊的方法：创造模式 → 卡牌袋里填满 27 张羊（手点 54 次比较痛苦）
- **建议后续做**（未实现，仅记录）：
  1. `/duel fillbag` 调试命令：一键把卡包填满当前注册的全部卡
  2. 或 P2 补默认卡包内容（`CARD_DESIGN.md` 的 37 张卡池）后自然解决
- 四类结算的新路径（魔法/秘密陷阱/装备）需要相应类型的卡才能实测；仅羊卡只能测"站场兜底"路径

### 0.3 视角与操作环境
- 对局中玩家被锁定在座位（不能移动/破坏/放置/丢弃），镜头强制俯视牌桌
- 所有操作靠鼠标点击（HUD 手牌 / 牌桌桌面 / 右下按钮），无需快捷键

---

## 1. 分阶段测试清单

### P1-1a · 入座与准备（已测 ✓）
| # | 操作 | 预期 |
|---|---|---|
| 1 | 单张牌桌空手右键 | 只提示"需要双人牌桌"，不入座 |
| 2 | 两张牌桌并排放置（double），空手右键 | 成为房主，弹出设置界面（法力封顶 1-99、生命 1-999，默认 10/30） |
| 3 | 设置界面点"取消（退出准备）" | 界面关闭 + 离座提示，牌桌回 IDLE |
| 4 | 设置界面按 ESC | 只关界面，保留座位；再右键可重开界面 |
| 5 | 第二玩家空手右键 | 入座为挑战者 |
| 6 | 双方持满 27 张卡包右键牌桌 | 各自"已准备"，桌面显示双方提交牌组合并 |
| 7 | 潜行右键（空手/持包） | 已入座者：离座/取消准备；未入座者：无提示（不刷屏） |
| 8 | 破坏牌桌 | 双方收到"牌桌已被破坏，已离座"，可立即去别的桌 |

### P1-1b · 回合引擎（未测）
| # | 操作 | 预期 |
|---|---|---|
| 1 | 双方提交后 | 双方收到"双方已就绪！房主右键牌桌开始对局" |
| 2 | 房主右键 | "对局开始！X 先手" + 换牌提示（现为真实换牌阶段，见 P1-4） |
| 3 | `/duel endturn` 轮流结束回合 | 每回合：法力上限+1（封顶于设置值）并回满、抽 1 张、提示"第 N 轮" |
| 4 | 手牌满 8 张再抽 | 抽到的卡烧入弃牌堆并提示 |
| 5 | 牌库抽空后继续抽 | 疲劳伤害递增（第 n 次扣 n 血） |
| 6 | HP 归零 | "对局结束！X 获胜"，牌桌回 WAITING（保留座位与上限），可立即再开局 |
| 7 | 对局中掉线 | 对方直接获胜 |
| 8 | `/duel status` | 显示 phase/法力封顶/生命上限/轮次/双方 HP/MP/手牌/牌库/疲劳/准备状态 |
| 9 | 对局中退出世界重进 | 对局状态从 NBT 恢复 |

### P1-2 · 俯视相机与 HUD（未测）
| # | 操作 | 预期 |
|---|---|---|
| 1 | 开局瞬间 | 镜头切到双桌中心正上方，完全垂直俯视 |
| 2 | 对局中 | 玩家固定在座位；原版快捷栏/经验条/血量/饥饿/准星消失 |
| 3 | 窗口下方 | 手牌平铺（后手多 1 张金色"硬币"），悬停放大 |
| 4 | 窗口上方 | 双方状态条（名字/生命/法力/牌库/疲劳），行动方金色边框；中央回合数 |
| 5 | 右下角 | 己方回合绿色"结束回合"按钮可点；对方回合置灰 |
| 6 | 对局结束 | 镜头恢复、HUD 消失、恢复移动 |

### P1-3 · 出牌与攻击（未测）
| # | 操作 | 预期 |
|---|---|---|
| 1 | 点击手牌 | 白色高亮选中，顶部提示"点击己方空槽打出" |
| 2 | 点击己方半场空槽 | 卡上场（扣 MP、手牌减一、桌面出现卡）；MP 不足提示"法力不足" |
| 3 | 点击己方半场已上场卡 | 黄色高亮选中（攻击模式） |
| 4 | 选中己方卡后点击对方卡 | 炉石式互伤（双方各受对方 ATK 伤害）；HP≤0 的卡进弃牌堆消失 |
| 5 | 选中己方卡后点击对方半场空处 | 打脸（对方玩家 HP -= 己方卡 ATK）；HP 归零判负 |
| 6 | 刚召唤的卡当回合攻击 | 提示"该召唤物本回合还不能攻击"（召唤失调） |
| 7 | 已攻击过的卡再次攻击 | 提示"该召唤物本回合已经攻击过了" |
| 8 | 后手使用硬币 | 法力 +1（不超过上限），硬币消失 |

### P1-4 · 换牌与硬币（未测）
| # | 操作 | 预期 |
|---|---|---|
| 1 | 开局后 | 双方进入换牌阶段：顶部提示"换牌：已选 x/2" |
| 2 | 点击手牌 | 黄色高亮，最多选 2 张；再点取消 |
| 3 | 点"确认换牌" | 选中牌放回牌库底、抽等量新牌；双方都确认后"对局继续"并进入先手回合 |
| 4 | 换牌时选硬币 | 被拒绝（硬币不可换，客户端不发送，服务端也校验） |

### P1-5 · 四类结算区分（未测）
| # | 操作 | 预期 |
|---|---|---|
| 1 | 打出一张召唤卡（summon） | 占己方空槽站场（同 P1-3） |
| 2 | 选中魔法卡（mana）点己方半场 | 立即结算内建效果（heal/harm/fire/lava/draw/mana 等）→ 进弃牌堆，不占槽 |
| 3 | 选中秘密陷阱（secret_*）点己方半场 | 进入秘密区（状态条"秘密 xN"+1），对方看不到内容；第 4 张时提示"秘密区已满" |
| 4 | 选中铁砧（anvil_*）点对方召唤物 | 立即对目标召唤物造成 N 伤害，卡进弃牌堆；点空处被拒绝 |
| 5 | 打出手持仙人掌/岩浆块（*_thorns） | 占己方槽站场；选中它攻击 → 提示"无法主动攻击"；对方攻击它 → 攻击方受 1 反伤 |
| 6 | 选中装备（equip）点己方召唤物 | 附着（状态条"装备 xN"+1）；再装备同槽 → 替换，旧装备进弃牌堆 |
| 7 | 装备后的召唤物攻击 | 攻击力 = 卡面 ATK + 装备 ATK |
| 8 | 有装备的召唤物受伤 | 先扣装备耐久（装备 HP），耐久归零 → 装备损毁进弃牌堆，剩余伤害扣本体 |
| 9 | 秘密触发 | 对方出牌→secret_mine 打玩家2；对方上场→secret_arrow 打召唤物3；对方攻击→secret_wither 打攻击方2；对方回合开始→secret_sculk 跳过抽牌；对方场上≥3→secret_tnt 双方全场3 |
| 10 | 不死图腾（totem_protect） | 打出后状态条显示"图腾"；下次致命伤害被抵挡，生命置 5，图腾消失 |
| 11 | 旧卡兜底 | 默认包羊卡（trap/skill=0）仍按召唤卡站场，行为与 P1-3 完全一致 |

---

## 2. 潜在 Bug / 风险清单（实现时已知，未实测）

### 2.1 高概率
| # | 位置 | 问题 | 症状 | 排查方法 |
|---|---|---|---|---|
| B1 | `HudClickManager.hitTable` | 屏幕→桌面映射的 **-Z 符号**（假设屏幕向上=世界北= -Z）。若符号反了，点击位置会上下镜像 | 点己方半场却命中对方半场（或反之） | 临时在代码里对 `ndcY` 取反；或先点桌角验证 |
| B2 | `DuelCameraManager` | AT 方法名若因映射差异未生效，`camera.setPosition` 会抛 `IllegalAccessError` | 开局瞬间崩溃 | `latest.log` 搜 `Access Transformer` 警告；对照 `Camera.java` 的 `setPosition(DDD)V / setPosition(Vec3)V / setRotation(FFF)V` |
| B3 | `DuelCameraManager.onRenderLevel` | 相机在 `AFTER_SKY` 改，天空已用旧相机渲染 | 屏幕边缘可能出现天空错位残影 | 可接受；若明显再考虑改 stage 或接受 |
| B4 | 手牌/硬币 | 硬币没有卡牌索引，任何查表都会失败 | 若硬币出现在战场/牌库逻辑里会变空卡 | 已按 `COIN_CARD_ID` 特判；出现"透明卡"即说明漏判 |

### 2.2 中概率
| # | 位置 | 问题 | 症状 |
|---|---|---|---|
| B5 | `DuelSeatLock` | 玩家被传送后落地/摔落重置；极端情况下每 tick 传送可能抖屏 | 座位处轻微抖动 |
| B6 | 牌桌朝向 | 双桌只支持南北向（`updateShape` 只设 NORTH/SOUTH）；东西向并排的两桌会互相把对方变回 false | 东西向摆桌无法开局 |
| B7 | `DuelTableBlockEntityRenderer` | 牌组 9×3 的间距参数（2.4/16 等）若视觉上太挤/重叠，是排版问题 | 桌面卡牌重叠 |
| B8 | 客户端 BE 数据 | 战场内容靠 `getUpdateTag` 同步；`sync()` 用 `sendBlockUpdated`，同 tick 高频变更（连抽/连打）可能丢中间帧 | 战场卡偶尔滞后一帧（最终一致） |
| B9 | 换牌同时点确认 | 客户端选中集合与服务端校验（≤2、不含硬币、合法索引）双保险，但点击过快可能先发后清 | 理论上服务端拒绝即无事 |
| B10 | 座位残留 | 玩家在对局中直接关闭游戏（非正常离座）→ 重进后 attachment 已清但牌桌 uuid 还在 | 房主重进后仍视为入座状态；重新入座或潜行右键可恢复 |

### 2.3 低概率 / 边界
| # | 问题 | 说明 |
|---|---|---|
| B11 | 跨维度/超远点击 | 所有服务端操作按座位 attachment 定位牌桌，不信任客户端坐标；出牌/攻击包本身不带坐标，安全 |
| B12 | 活塞推动牌桌 | 会触发"牌桌已被破坏"清座逻辑（行为可接受，见 P1-1a 复查记录） |
| B13 | 单人世界开局 | 需要两名玩家；单人无法开局（设计如此） |
| B14 | 法力封顶设 1 | 双方永远 1 点法力，高费卡无法打出（规则允许，非 bug） |
| B15 | 手牌为 0 时点"结束回合" | 正常流程，无碍 |
| B16 | `DuelEngine.checkSecretsOnPlay/OnAttack` | 秘密触发内的伤害可能终结对局（`finishDuel`→`resetDuel` 清空陷阱区与弃牌堆），循环用快照迭代 + 每轮校验 phase，但触发后仍会把陷阱加入已重置的弃牌堆 | 对局结束时弃牌堆残留 1 张陷阱卡（下次开局 resetDuel 清掉，无害） |
| B17 | `applyCardDamage` 装备吸收 | 反伤/互伤/魔法伤害都走装备耐久，逻辑统一；但"伤害恰好等于装备耐久"时装备损毁、本体无伤 | 属预期规则，非 bug；确认消息"装备损毁"出现即可 |
| B18 | 装备替换 | 替换时旧装备进弃牌堆，但旧装备此前被吸收掉的耐久不会恢复（丢弃的是磨损后的装备） | 属预期（简化）；P2 做独立装备槽时再考虑 |
| B19 | 图腾边界 | `totem_saved` 生命置 5 且封顶 hpCap；若 hpCap<5 则回复到 hpCap；伤害低于剩余生命时图腾不触发 | 属预期规则 |
| B20 | `HudClickManager.selectedHandKind` | 客户端按 type/skill 判定分支，与服务端 `playCard` 分支必须一致；若卡包 JSON 的 type 写错（如魔法卡误写 summon），客户端提示与结算都会按召唤走 | 卡包数据录入时注意 type 字段 |

---

## 3. 已知局限（有意为之，非 bug）

1. **四类结算已分流，但技能为最小内建集**：召唤/魔法/陷阱/装备走不同路径（站场/即时/秘密/附着），技能仅实现 P1 内建子集（heal/harm/fire/lava/draw/mana/golden_heal/pearl_strike/totem_protect/anvil/secret×5/thorns/装备数值+耐久）；战吼/亡语/嘲讽/冲锋/风怒/剧毒/潜行等完整技能系统在 P2（`SkillHooks` 已预留 6 个钩子）
2. **无弃牌堆洗回**：弃牌堆目前只在数据与死亡时使用，抽空只走疲劳（用户已确认规则）
3. **HUD 无卡面属性文字**（HP/ATK 数字、法力水晶、伤害飘字、音效）：P1-5 打磨项
4. **战斗数值**：默认卡包只有 1/1/1 的羊，互伤即互死，战斗测试需要 37 张卡池（`CARD_DESIGN.md`，P2 制作）
5. **攻击目标无世界内高亮**：选中后靠顶部文字提示，P1-5 可加
6. 掉线直接判负，无重连（用户已确认）
7. **拖拽施法交互未实现**（用户已确认"先记录下来"）：炉石式目标选择——长按拖动手牌/玩家头像/召唤物移动到目标上施法，拖动时绘制指示箭头；P2 交互重构时实现（见 `CARD_DESIGN.md` 第 8 节）

---

## 4. 测试用命令速查

| 命令 | 作用 |
|---|---|
| `/duel endturn` | 结束当前回合（HUD 按钮的等价物） |
| `/duel status` | 查看本桌完整状态 |
| `/duel leave` | 离座（对局中会拒绝） |

## 5. 回归测试建议顺序

P1-1a 全套 → P1-1b 命令流程 → P1-2 视觉 → P1-4 换牌 → P1-3 出牌攻击 → **P1-5 四类结算**（需对应类型的卡）→ 完整一局（含疲劳与胜负）。
